//
//  ViewController.swift
//  UOW Booking
//
//  Created by Elroy C. on 25/10/21.
//

import UIKit

class LoginViewController: UIViewController{
    
    override func viewDidLoad() {
        super.viewDidLoad()
    }
/*   func validateField() -> String? {
      
       if usernameField.text?.trimmingCharacters(in: .whitespacesAndNewlines) == "" ||
            passwordField.text?.trimmingCharacters(in: .whitespacesAndNewlines) == "" {
            
            showAlert()
        }
        
        return nil
    }
    
    @IBAction func didTapbutton() {
        let error = validateField()
        if error != nil {
            showAlert()
        }
        else {
        }
    }
    
    func showAlert(){
        let alert = UIAlertController(title: "Incorrect Details", message: "Please Re-Enter Details", preferredStyle: .alert)
        
        alert.addAction(UIAlertAction(title: "Dismiss", style: .cancel, handler: { action in
            print("Tapped Dismiss")
        }))
        
        present(alert, animated: true)
    }
}*/
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
    }
}

